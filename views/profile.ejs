<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-h-screen text-white bg-zinc-900 flex flex-col items-center justify-center">
        <a href="/logout" class="bg-red-500 p-2 rounded-md  mb-[20px]">logout</a>
        <div class="flex gap-2 items-center justify-center">
            <div class="w-10 h-10 rounded-md bg-red-500">
                <img src="/images/uploads/<%=user.profilePic%>" class="w-full h-full object-cover">
            </div>
            <h1 class="text-white mb-[20px] text-2xl ">Hey, <%=user.username%> 👋👋</h1>
        </div>
        
        <h2 class="mb-[20px]">You can create a new post</h2>

        <form action="/post" method="post"  class="flex flex-col items-center justify-center" >
              <textarea name="content"  placeholder="what's in your mind? " class="w-[300px] h-[200px] p-[10px] rounded-md text-gray-500 outline-none resize-none"></textarea>
              <input type="submit" value="create new post" class="bg-blue-500 w-[300px] rounded-md p-2 mt-[20px]">
        </form>
           
      <div class="flex flex-col items-center justify-center">
            <h1 class="mt-[20px] text-xl mb-[20px]">your posts</h1>
            <div class="flex flex-wrap gap-3">
                <%user.posts.reverse().forEach((post)=>{%>
                    <div class="bg-zinc-600 h-[150px] w-[350px] rounded-md p-3">
                        <h1 class="text-blue-500 mb-2"><%=user.username%></h1>
                        <p class="tracking-tight text-white"><%=post.content%></p>
                         <small><%=post.likes.length%></small>
                        <div class="flex justify-between mt-[20px]">
                             <a href="/like/<%=post._id%>" class="text-blue-300">
                                <%=post.likes.indexOf(user._id)===-1?'like':'Unlike'%>
                             </a>
                             <a href="/edit/<%=post._id%>">Edit</a>
                        </div>
                 </div>
                <%})%>
                  
            </div>
      </div>
    </div>
</body>
</html>